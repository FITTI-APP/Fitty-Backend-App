extend type Mutation{
    """
    팔로우를 추가합니다. 성공 여부를 반환합니다.
    """
    followUser(followeeId: Long!): Follow!

    """
    팔로우를 삭제합니다. 성공 여부를 반환합니다.
    """
    unfollowUser(followeeId: Long!): Boolean!
}
